import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    purple: {
      primary: "#6C63FF",
    },
    lightPurple: {
      main: "#FEEBFF",
    },
    gray: {
      main: "#2C323A",
    },
    darkPurple: {
      main: "#38182F",
    },
  },
});
